//--------------------------------------------------------
//-- Wrapper for web build
//--------------------------------------------------------

import all from '../index.js';

window.pinki = all;  // eslint-disable-line
